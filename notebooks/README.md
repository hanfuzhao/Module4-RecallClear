# Notebooks

Exploration space. Nothing here is part of the pipeline — every step that
produces a committed artefact lives in `scripts/` and is reachable from
`setup.py` or the `Makefile`, so results stay reproducible from a clean clone.

Corpus statistics that would normally live in an exploratory notebook are
generated by `make features` into `data/outputs/dataset_stats.json`.
